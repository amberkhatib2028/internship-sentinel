# Copy to deploy/config.env and fill in. That file is gitignored, which keeps
# your address out of version control.
SENTINEL_TO_EMAIL=you@example.com

# Optional overrides.
# AWS_REGION=us-east-1
# FUNCTION_NAME=swe-intern-sentinel
# S3_BUCKET=my-own-bucket
